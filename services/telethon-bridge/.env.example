# Shared secret — must match BRIDGE_TOKEN in GianCore .env
BRIDGE_TOKEN=change-me-to-a-random-secret

# Session files storage (leave default when using Docker volume)
SESSIONS_DIR=/app/sessions

# Default reply timeout in seconds
REPLY_TIMEOUT=45
